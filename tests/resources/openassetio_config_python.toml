# This config sets up the BasicAssetLibrary (aka BAL) Python
# manager/plugin for use in the main test cases.

[manager]
identifier = "org.openassetio.examples.manager.bal"

[manager.settings]
library_path = "${config_dir}/integration_test_data/bal_library.json"
